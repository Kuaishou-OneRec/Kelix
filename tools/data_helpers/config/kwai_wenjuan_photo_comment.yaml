output_dir: /llm_reco/huqigen/dataset/kwai_wenjuan_photo_comment/20250206
shard_size: 10000
dataset:
  class_name: ParquetDataset
  kwargs:
    path: viewfs://hadoop-lt-cluster/home/reco_kaiworks/dw/reco_kaiworks.db/reco_llm_wenjuan_comment_sample_0210_tmp
    columns:
      - photo_id
      - wenjuan_type
      - caption
      - user_comment
      - ocr
      - asr

converter:
  class_name: KwaiWenJuanCaptionConverter
  kwargs:
    source: kwai_wenjuan_photo_comment
    video_dir: /mmu_mllm_hdd/huqigen/wenjuan_sft/photo_0210_11w
    ffmpeg_args: -vf scale='if(gt(iw,ih),min(480,iw),-2)':'if(gt(ih,iw),min(480,ih),-2)' -r 4 -t 60 -c:a aac -c:v libx264 -b:a 55k -pix_fmt yuv420p -probesize 200M -analyzeduration 200M
    prompts:
      - "你是一个短视频平台的内容理解专家，请结合短视频的视频抽帧结果、ocr内容、asr内容以及这个视频在短视频平台的热门评论信息，判断用户是否会对这个视频满意。请充分考虑评论信息，结合这些信息提炼出该视频所有的看点。请在回答的最后给出最终的满意度判断结果，只包含满意/不满意两种类型，格式为：【结果：满意/不满意】"
    output_file_path: /mmu_mllm_hdd/huqigen/wenjuan_sft/photo_0210_11w/prompt_result.json
    