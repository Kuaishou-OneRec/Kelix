output_dir: viewfs://hadoop-lt-cluster/home/reco_wl/mpi/huqigen/recovlm_dataset/wenjuan_sft/i2i_benchmark_dataset_v1
split_size: 536870912
dataset:
  class_name: csv
  path: /llm_reco/maosiyang/dataset/i2i/triplets_imf.csv

converter:
  class_name: i2iConverter
  kwargs:
    source: kwai_cf_i2i_pair
    video_dir: /llm_reco/luoxinchen/dataset/InHouse/Photo/20250215/480p_60s_4fps_v2/
    ffmpeg_args: -vf scale='if(gt(iw,ih),min(480,iw),-2)':'if(gt(ih,iw),min(480,ih),-2)' -r 4 -t 60 -c:a aac -c:v libx264 -b:a 55k -pix_fmt yuv420p -probesize 200M -analyzeduration 200M
    prompts:
      - "你是一个短视频内容分析专家，现在需要你判断一下平台内两个视频的相似度，我们会提供源视频的视频内容以及两个候选视频的视频内容，视频内容特征将以OCR/ASR/图像数据呈现，请判断出两个候选视频中哪个和源视频最相似，请在回答的最后给出最终的相似度判断结果。格式为【和源视频最相似的视频是 【视频1 / 视频2】】"
    cot_txt_file_path: /mmu_mllm_hdd/dengjiaxin03/11w_data_infer_result_with_all_cmt
    max_frames_per_video: 20
    enable_debug: true
    filter_by_llm_label: false
    enable_llm_response: false