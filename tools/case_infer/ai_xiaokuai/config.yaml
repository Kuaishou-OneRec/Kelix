# 数据路径
data_path: /llm_reco/chuchenglong/R3/Ai_kwai/data/vqa_query_testdata_1500.json

# 输出目录
output_dir: /llm_reco/chuchenglong/R3/Ai_kwai/output_msy_0.3_pt

# 保存间隔（处理多少条数据后保存一次）
save_interval: 10

# 模型配置
model:
  path: /llm_reco_ssd/luoxinchen/output3/RecoVLM-Base/0.3.0/cmt/global_step4001/merged_4001
  limit_mm_per_prompt: 30
  tp: 1

# 采样参数
sampling:
  temperature: 0.7
  top_p: 0.9
  repetition_penalty: 1.0
  max_tokens: 2048

# 数据加载参数
dataloader:
  batch_size: 1
